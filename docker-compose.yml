services:
  app:
    build: .
    volumes:
      - ./src:/var/www/html  # srcフォルダをコンテナのWeb公開場所にマウント
    ports:
      - "8080:80"            # これでブラウザからアクセス可能に
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: test_db
      MYSQL_ROOT_PASSWORD: root